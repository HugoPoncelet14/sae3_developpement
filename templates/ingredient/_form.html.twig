{{ form_start(form) }}
    {{ form_row(form.nomIng) }}
    {{ form_row(form.imgIng) }}
    {{ form_row(form.allergene) }}
    <input class="auth-button custom-size" type="submit" value={{ submit_label }}>
{{ form_end(form) }}